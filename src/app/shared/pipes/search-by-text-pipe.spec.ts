import { SearchByTextPipe } from './search';

describe('SearchByTextPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchByTextPipe();
    expect(pipe).toBeTruthy();
  });
});
